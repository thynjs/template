<script>
  const count = $signal(0);
  $effect(() => {
    console.log(`Count is ${count.get()}`);
  });
</script>

<button onclick={() => count.update(c => c + 1)}>
  Count: {{ count.get() }}
</button>

<style>
  button {
    background: #333;
    color: #fff;
    border: 0;
    padding: 5px 10px;
    cursor: pointer;
  }
</style>
